interface:
  display_name: "Workaholoop"
  short_description: "Repo-specific patterns and workflows for workaholoop"
  default_prompt: "Use the workaholoop repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true