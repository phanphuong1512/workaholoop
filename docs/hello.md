# `wlp hello` Command

The `wlp hello` command is a simple utility that prints a greeting message.

## Usage

```bash
wlp hello
```

## Description

This command simply outputs `Hello from workaholoop!` to the console. It can be used to verify that the CLI is installed and functioning correctly.
